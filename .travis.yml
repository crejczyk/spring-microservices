language: java
jdk:
 - oraclejdk8
sudo: false

cache:
  directories:
  - $HOME/.m2
    
install: true
    
#script: "mvn clean verify"
after_success:
  - mvn clean verify -Pjacoco org.eluder.coveralls:coveralls-maven-plugin:report